The Oding Project about data structure: **(banlanced) Binary Search Tree.**
The feature are as follows:
1. create a data structure: banlanced binary search tree(BST).
2. the Class will have method: `includes(value)`, `insert(value)`, `deleteItem(value)`, `levelOrderForEach(callback)`, `inOrderForEach(value)`, `preOrderForEach(value)`, `postOrderForEach(value)`, `height(value)`, `depth(value)`, `isBalanced(value)`, `reBalance()`
3. Link:https://chatgpt.com/c/69e03f33-4af4-83ea-b1b7-401af707e49c